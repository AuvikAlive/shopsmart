import React from 'react'
import StyledGetStartedSection from './StyledGetStartedSection'
import Title from './Title'
import Subtitle from './Subtitle'
import Button from '../button/Button'

const GetStartedSection = () => (
  <StyledGetStartedSection>
    <Title>Ready to get started?</Title>
    <Subtitle>
      If you're running a small bodega or a chain of shopping malls, contact us
      for a custom proposal and to learn more. We're happy to help.
    </Subtitle>
    <Button>GET STARTED</Button>
  </StyledGetStartedSection>
)

export default GetStartedSection
